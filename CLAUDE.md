# 个人记账（Personal Ledger）— 项目文档

> 本文档是本项目的「唯一事实来源」。每次会话开始时，Claude Code 必须先阅读本文档。

## 一、项目简介

「个人记账」是一款运行在 Windows 和 macOS 电脑上的桌面记账 APP，帮助用户记录每一笔支出和收入（货币：人民币），支持两级分类管理账目、查看统计报表。数据保存在本机，无需联网。

- 项目位置：D:\个人记账\personal-ledger
- 目标用户：完全不懂编程的普通用户（本人）
- 界面语言：中文

## 二、产品文档

### 1. 产品定位

简单、可靠、开箱即用的个人桌面记账工具。不做社交、不含广告、不上传任何数据。

### 2. 功能范围

**MVP（第一版，必须全部完成）：**

1. **记一笔**：金额、收支类型（支出/收入）、两级分类（必选）、日期（默认今天）、备注（可选）；页面为全屏宽排版；二级小类下拉框末尾有「➕ 自定义输入…」选项，可直接输入自定义小类名（自动保存为新小类，下次可选）
2. **账单列表**：按时间倒序展示；支持按月份、收支类型、分类、关键词筛选；支持编辑、删除（带确认弹窗）
3. **统计报表**（整页两种模式切换）：
   - 「📊 月总占比」模式：只显示某一个月 —— 月统计卡片（本月支出/收入/结余）、近 12 个月收支趋势图、本月收支环形图、本月支出分类占比饼图 + 明细表；日期选择器选月份
   - 「📆 年总占比」模式：只显示某一年 —— 年统计卡片（年支出/年收入/年结余）、该年 12 个月收支趋势图（独立的图）、全年收支环形图；日期选择器只选年份
4. **分类管理**：查看/新增/修改/删除分类；所有分类均可编辑删除（有记账记录的、或还包含小类的分类不可删除）
5. **数据管理**：CSV 导出备份；「选择备份文件夹」一键把数据库完整备份到用户指定的文件夹（VACUUM INTO 在线快照，数据完整一致）

**二期（暂不做，未来再议）：** 云同步（Win/Mac 数据互通）、预算提醒、图片凭证、深色模式、多账本

### 3. 分类体系（两级：一级大类 → 二级小类）

内置默认分类如下（用户可自行增删改）：

**支出分类（10 个一级大类）：**

| 一级大类 | 二级小类 |
|---|---|
| 餐饮饮食 | 早餐、午餐、晚餐、零食饮料、外卖、聚餐 |
| 交通出行 | 公交地铁、打车、加油充电、停车费、火车票、飞机票 |
| 购物消费 | 日用百货、服饰鞋包、数码家电、美妆护肤 |
| 居住住房 | 房租月供、水电燃气、物业费、家居用品、维修装修 |
| 通讯网络 | 手机话费、宽带上网、会员订阅 |
| 娱乐休闲 | 电影演出、游戏充值、旅游度假、运动健身 |
| 医疗健康 | 看病买药、体检保健 |
| 教育学习 | 书籍资料、课程培训、考试报名、文具用品 |
| 人情往来 | 红包礼金、请客送礼、孝敬父母、捐款 |
| 其他支出 | 其他（记一笔时可输入任意自定义小类名，自动保存） |

**收入分类（5 个一级大类）：**

| 一级大类 | 二级小类 |
|---|---|
| 工资薪金 | 基本工资、奖金绩效、补贴报销 |
| 理财投资 | 存款利息、基金股票收益、房租收入 |
| 兼职副业 | 兼职收入、稿费、二手转卖 |
| 人情往来 | 收到红包、礼金返还 |
| 其他收入 | 其他 |

### 4. 数据与隐私

- 所有数据保存在本机（Windows：`C:\Users\<用户名>\AppData\Roaming\com.personalledger.app\ledger.db`；macOS：`~/Library/Application Support/com.personalledger.app/ledger.db`）
- 不上传任何数据、不联网同步（二期再议）
- 备份方式：CSV 导出，或「设置 → 选择备份文件夹」完整备份数据库

## 三、技术选型（已由用户拍板）

| 项 | 选定 | 说明 |
|---|---|---|
| 桌面框架 | Tauri 2.x | 安装包约 10MB，启动快、省内存 |
| 前端 | Vue 3 + TypeScript + Vite | |
| UI 组件库 | Element Plus | 中文生态最好，表格/表单/日期选择开箱即用 |
| 图表 | ECharts | 饼图/折线图全覆盖，中文文档完善 |
| 状态管理 | Pinia | 分类等数据跨页面共享 |
| 路由 | vue-router（v4 稳定版） | 5 个页面切换 |
| 数据存储 | SQLite（tauri-plugin-sql） | 本地单文件数据库，事务安全、查询统计方便 |
| 金额处理 | 整数「分」存储 | 从源头避免小数误差 |

当初给用户的备选方案对比（存档）：Electron + Vue（最成熟但安装包约 100MB、较吃内存）；Flutter 桌面版（界面精美但工具链更大）；网页应用（无安装包但数据存浏览器有丢失风险）。

## 四、协作规则（重要！）

1. **技术决策必须由用户拍板**：用户不懂编程，无法提供具体技术要求。任何技术方案的选择，Claude Code 必须先：① 列出至少 2 个可行方案；② 用通俗易懂的语言（少用专业术语，多用类比）解释每个方案的优劣势；③ 给出明确推荐；④ 等用户决定后再动手。未经用户决定，不得擅自选定或更换技术方案。
2. **产品决策同样先展示方案**：涉及功能范围、界面布局、分类设计等，先说明方案再实施。
3. **动手前先说明**：每次做改动前，用通俗语言说清楚「要做什么、为什么做、影响什么」。
4. **分工**：用户负责拍板决策和试用反馈；Claude Code 负责环境搭建、写代码、测试、打包。
5. **里程碑式推进**：完成一个功能模块 → 用户试用验收 → 确认后再做下一个。不要一口气做完所有功能。
6. **遇到任何问题**：如实向用户报告，用通俗语言解释原因和可选解决方案，让用户选。
7. **发布节奏**：每次功能改动完成后，先启动开发版让用户测试体验；用户明确确认没问题后，才打包并发布新版本到 GitHub（v0.1.1、v0.1.2……）。未经用户确认不得发布。

## 五、开发环境与常用命令

- 开发环境：Windows 11 本机；Node.js 24 LTS + Rust（已配 rsproxy 镜像）+ MSVC Build Tools
- 启动开发模式：`npm run tauri dev`（首次编译 10-30 分钟属正常）
- 打包 Windows 安装包：`npm run tauri build`（打包目标已设为仅 NSIS；MSI 因本机缺 WiX/VBSCRIPT 组件已放弃，个人使用 NSIS 足够。产物：src-tauri\target\release\bundle\nsis\）
- 应用图标：由 D:\个人记账\personal-ledger\app-icon.png 生成（`npm run tauri icon <图片>` 重新生成全平台图标；换图标只需替换该 PNG 再重跑此命令）
- 待办（Mac 二期）：identifier `com.personalledger.app` 以 .app 结尾在 macOS 有警告，做 Mac 版时需改名并迁移数据目录
- npm 镜像 registry.npmmirror.com、crates 镜像 rsproxy.cn（均已配置）
- 数据库文件位置见「数据与隐私」一节

## 六、开发约定（Claude Code 内部遵循）

- 金额：前端字符串解析「元→分」，禁止浮点乘法；展示用 `toFixed(2)`
- SQL 占位符用 `$1`/`$2`（SQLite 插件），不是 `?`
- Tauri 2 权限在 `src-tauri/capabilities/default.json` 声明（sql/dialog 插件权限）
- 数据库连接用模块级单例复用
- CSV 导出加 UTF-8 BOM 防 Excel 中文乱码
- 被交易引用的分类不可删除；还包含小类的一级大类不可删除
- 迁移文件一旦被应用就**不可修改**（插件会校验 checksum，修改导致启动崩溃「migration was previously applied but has been modified」）；任何数据变更都只能新增迁移版本（0002、0003……）
